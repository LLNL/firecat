from setuptools import setup

setup(name="FireCat",
        version="0.0.1",
        description="Coupling microkinetics and continuum models for electrochemistry",
        packages=["firecat"],
        install_requires=["numpy"],
        )
